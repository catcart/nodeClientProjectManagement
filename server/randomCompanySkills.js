/**
 * Created by usuario on 9/23/15.
 */


function initializeFruit() {
    // take out math.floor for more decimal places
    return Math.floor(Math.random() * (1 + 60 - 10) + 10);
};


function initializeFruit() {
    // take out math.floor for more decimal places
    return Math.floor(Math.random() * (1 + 60 - 10) + 10);
};


function initializeFruit() {
    // take out math.floor for more decimal places
    return Math.floor(Math.random() * (1 + 60 - 10) + 10);
};